{% extends 'base.html.twig'%}

{%block body %}

</br><center><h4>Liste des Equipements</h4></center></br>

<table class="table">
        <thead>
          <tr>
            <th>Nom</th>
            <th>Quantite</th>
            <th>Voir Detail</th>
         </tr>
       </thead>
     {% for footEquipement in foot %}
  
        <tbody>
          <tr>
            <td>{{footEquipement.name}}</td>
            <td>{{footEquipement.quantite}}</td>
          <td><button class="btn btn-secondary my-2 my-sm-0" type="submit" onclick="window.location.href = '{{path('nom_equipement-all', {'id':footEquipement.id}) }}';">Voir</button></td>
           
          </tr>


          </tbody>

          {% endfor %}
          </table>
         
 


{% endblock %}
